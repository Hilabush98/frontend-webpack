const headerStyle = {
    textAlign: 'center',
    height:'10vh',
    paddingInline: 48,
    backgroundColor: '#fff',
  };
  const contentStyle = {
    textAlign: 'center',
    minHeight: 120,
    height:'80vh',
    backgroundColor: '#fff',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  };

  const footerStyle = {
    textAlign: 'center',
    height:'10vh',
    backgroundColor: '#fff',
  };
  const layoutStyle = {
    borderRadius: 8,
    overflow: 'hidden',
  };

  export {
    headerStyle,
    contentStyle,
    footerStyle,
    layoutStyle
  }